'use client';
import { useEffect,useMemo,useState } from 'react';
type Post={id:string|number;author?:{username:string};name?:string;country?:string|null;city?:string|null;location?:string;category:string;title:string;content?:string;body?:string;_count?:{comments:number;helpOffers:number};help?:number;comments?:number;status:string};
const seed:Post[]=[{id:1,name:'Amina K.',location:'Lahore, Pakistan',category:'Education',title:'Looking for a used laptop for my studies',body:'I am learning web development and need a reliable laptop.',help:18,comments:7,status:'Active'}];
export default function Home(){const [posts,setPosts]=useState<Post[]>(seed),[open,setOpen]=useState(false),[category,setCategory]=useState('All'),[authOpen,setAuthOpen]=useState(false),[mode,setMode]=useState<'login'|'register'>('login'),[user,setUser]=useState<any>(null),[busy,setBusy]=useState(false),[error,setError]=useState('');const [form,setForm]=useState({title:'',content:'',category:'General',country:'',city:''});const [auth,setAuth]=useState({email:'',username:'',password:''});const [action,setAction]=useState<{kind:'comment'|'report',id:string}|null>(null);const [text,setText]=useState('');
useEffect(()=>{fetch('/api/auth/me').then(r=>r.ok?r.json():null).then(x=>x?.user&&setUser(x)).catch(()=>{});fetch('/api/posts').then(r=>r.ok?r.json():null).then(x=>x?.posts?.length&&setPosts(x)).catch(()=>{})},[]);const visible=useMemo(()=>category==='All'?posts:posts.filter(p=>p.category===category),[posts,category]);function login(){setMode('login');setAuthOpen(true)}
async function submitAuth(){setBusy(true);setError('');try{const url=mode==='login'?'/api/auth/login':'/api/auth/register';const payload=mode==='login'?{email:auth.email,password:auth.password}:auth;const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}),x=await r.json();if(!r.ok)throw new Error(x.error||'Request failed');setUser(x.user);setAuthOpen(false)}catch(e:any){setError(e.message)}finally{setBusy(false)}}
async function publish(){if(!user){setOpen(false);login();return}setBusy(true);try{const r=await fetch('/api/posts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)}),x=await r.json();if(!r.ok)throw new Error(x.error);setPosts([x.post,...posts]);setOpen(false)}catch(e:any){setError(e.message)}finally{setBusy(false)}}
async function help(id:string){if(!user)return login();const r=await fetch(`/api/posts/${id}/help`,{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});if(!r.ok)return alert('Unable to offer help.');setPosts(posts.map(p=>String(p.id)===id?{...p,_count:{comments:p._count?.comments??p.comments??0,helpOffers:(p._count?.helpOffers??p.help??0)+1}}:p));}
async function submitAction(){if(!action)return;if(!user){setAction(null);return login()}setBusy(true);try{const endpoint=action.kind==='comment'?`/api/posts/${action.id}/comments`:`/api/posts/${action.id}/report`;const body=action.kind==='comment'?{content:text}:{reason:text||'Community safety concern'};const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),x=await r.json();if(!r.ok)throw new Error(x.error||'Request failed');if(action.kind==='comment')setPosts(posts.map(p=>String(p.id)===action.id?{...p,_count:{comments:(p._count?.comments??p.comments??0)+1,helpOffers:p._count?.helpOffers??p.help??0}}:p));setAction(null);setText('');}catch(e:any){setError(e.message)}finally{setBusy(false)}}
async function logout(){await fetch('/api/auth/logout',{method:'POST'});setUser(null)}
return <main><header className="nav"><a className="brand" href="#top"><span>♥</span> MADAD</a><nav><a href="#feed">Explore</a>{user?<><span className="user">Hi, {user.username}</span><button className="ghost" onClick={logout}>Sign out</button></>:<button className="ghost" onClick={login}>Sign in</button>}<button onClick={()=>user?setOpen(true):(setMode('register'),setAuthOpen(true))}>{user?'Post a need':'Join MADAD'}</button></nav></header><section id="top" className="hero"><div className="eyebrow">A GLOBAL COMMUNITY FOR PRACTICAL HELP</div><h1>One message can<br/><em>start a chain of help.</em></h1><p>Share what you need. Discover people who can help. Build a kinder, more connected world — safely.</p><button className="primary" onClick={()=>user?setOpen(true):(setMode('register'),setAuthOpen(true))}>Post your need →</button></section><section id="feed" className="feed-wrap"><aside><h3>Explore</h3>{['All','Education','Work','Community','Medical','Housing','General'].map(c=><button key={c} className={category===c?'selected':''} onClick={()=>setCategory(c)}>{c}</button>)}</aside><section className="feed"><div className="feed-head"><div><span className="eyebrow">COMMUNITY FEED</span><h2>People helping people.</h2></div><button className="primary small" onClick={()=>user?setOpen(true):login()}>+ Create post</button></div>{visible.map(p=>{const name=p.author?.username||p.name||'Community member',body=p.content||p.body||'',location=[p.city,p.country].filter(Boolean).join(', ')||p.location||'Location not shared',helpCount=p._count?.helpOffers??p.help??0,commentCount=p._count?.comments??p.comments??0;return <article className="post" key={p.id}><div className="avatar">{name[0]}</div><div className="post-main"><strong>{name}</strong><span> · {location}</span><div className="tags"><span>{p.category}</span><span className="active">{p.status}</span></div><h3>{p.title}</h3><p>{body}</p><div className="post-actions"><button onClick={()=>help(String(p.id))}>🤝 I can help <b>{helpCount}</b></button><button onClick={()=>setAction({kind:'comment',id:String(p.id)})}>💬 {commentCount}</button><button className="report" onClick={()=>setAction({kind:'report',id:String(p.id)})}>Report</button></div></div></article>})}</section></section>{open&&<div className="modal-backdrop"><div className="modal"><button className="close" onClick={()=>setOpen(false)}>×</button><h2>What do you need help with?</h2><label>Title<input value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>Message<textarea value={form.content} onChange={e=>setForm({...form,content:e.target.value})}/></label><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{['General','Education','Work','Community','Medical','Housing'].map(x=><option key={x}>{x}</option>)}</select></label>{error&&<p className="notice">{error}</p>}<button className="primary full" disabled={busy} onClick={publish}>Submit post →</button></div></div>}{action&&<div className="modal-backdrop"><div className="modal"><button className="close" onClick={()=>setAction(null)}>×</button><h2>{action.kind==='comment'?'Add a comment':'Report this post'}</h2><textarea value={text} onChange={e=>setText(e.target.value)} placeholder={action.kind==='comment'?'Write a helpful comment...':'Tell us why this post should be reviewed...'}/><button className="primary full" disabled={busy} onClick={submitAction}>{action.kind==='comment'?'Post comment':'Submit report'}</button></div></div>}{authOpen&&<div className="modal-backdrop"><div className="modal"><button className="close" onClick={()=>setAuthOpen(false)}>×</button><h2>{mode==='login'?'Welcome back':'Join MADAD'}</h2>{mode==='register'&&<label>Username<input value={auth.username} onChange={e=>setAuth({...auth,username:e.target.value})}/></label>}<label>Email<input value={auth.email} onChange={e=>setAuth({...auth,email:e.target.value})}/></label><label>Password<input type="password" value={auth.password} onChange={e=>setAuth({...auth,password:e.target.value})}/></label>{error&&<p className="notice">{error}</p>}<button className="primary full" onClick={submitAuth}>{mode==='login'?'Sign in':'Create account'}</button></div></div>}</main>}
