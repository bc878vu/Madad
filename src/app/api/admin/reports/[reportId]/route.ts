import { NextResponse } from 'next/server';import { prisma } from '@/lib/prisma';import { requireModerator } from '@/lib/admin';
export async function PATCH(request:Request,{params}:{params:Promise<{reportId:string}>}){if(!await requireModerator())return NextResponse.json({error:'Forbidden'},{status:403});const {reportId}=await params;const {action}=await request.json();if(!['DISMISS','REMOVE_POST','RESTRICT_AUTHOR','UNDER_REVIEW'].includes(action))return NextResponse.json({error:'Invalid action'},{status:400});const report=await prisma.report.findUnique({where:{id:reportId},include:{post:true}});if(!report)return NextResponse.json({error:'Not found'},{status:404});await prisma.$transaction(async tx=>{if(action==='REMOVE_POST')await tx.post.update({where:{id:report.postId},data:{status:'REMOVED'}});if(action==='UNDER_REVIEW')await tx.post.update({where:{id:report.postId},data:{status:'UNDER_REVIEW'}});if(action==='RESTRICT_AUTHOR')await tx.user.update({where:{id:report.post.authorId},data:{isRestricted:true}});await tx.report.update({where:{id:reportId},data:{status:action==='UNDER_REVIEW'?'REVIEWING':action==='DISMISS'?'DISMISSED':'RESOLVED'}})});return NextResponse.json({ok:true})}